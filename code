import java.util.ArrayList;
import java.util.TreeSet;

class Train {
    private ArrayList<Bogie> bogies = new ArrayList<>();
    private TreeSet<String> bogieIds = new TreeSet<>();

    // Add bogie with sorting + uniqueness
    public void addBogie(Bogie bogie) {

        if (bogieIds.contains(bogie.id)) {
            System.out.println(" Duplicate ID not allowed: " + bogie.id);
            return;
        }

        bogies.add(bogie);
        bogieIds.add(bogie.id);

        System.out.println(" Bogie added: " + bogie.id);
    }

    // Display sorted IDs
    public void displaySortedIds() {
        System.out.println("\n--- Sorted Bogie IDs ---");
        for (String id : bogieIds) {
            System.out.println(id);
        }
    }

    // Display actual train composition
    public void displayBogies() {
        System.out.println("\n--- Train Composition ---");
        for (Bogie b : bogies) {
            b.displayDetails();
        }
    }
}
